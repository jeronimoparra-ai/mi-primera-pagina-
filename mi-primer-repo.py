#Unidad 1
"""
Los lenguajes de programación (Python, Java) permiten dar instrucciones 
a la computadora mediante algoritmos: recetas finitas y ordenadas para 
resolver problemas (entrada → procesamiento → salida). 

Usamos IDEs como Google Colab para escribir y probar código en la nube 
sin instalaciones. 

Antes de programar, diseña el algoritmo paso a paso con instrucciones 
claras y una condición de finalización. 

Todo algoritmo tiene tres partes: entrada de datos, procesamiento y salida. 

En el código usamos variables (valores que cambian) y constantes (valores 
fijos) con tipos de dato específicos (entero, real, texto, booleano) que 
determinan las operaciones válidas.
"""
#Ejemplo de codigo en python
A = 34
B = 12
C = A + B
print(C)
